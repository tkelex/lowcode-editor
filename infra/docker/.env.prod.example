# Copy this file to infra/docker/.env before running:
# docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.prod.example.yml up -d --build

POSTGRES_USER=lowcode_app
POSTGRES_PASSWORD=replace-with-a-strong-database-password
POSTGRES_DB=lowcode_editor

# Server runtime env.
JWT_SECRET=replace-this-with-a-random-secret-of-at-least-32-characters
JWT_EXPIRES_IN=7d
FRONTEND_ORIGIN=https://lowcode.example.com
UPLOAD_DIR=/workspace/apps/api-server/uploads
UPLOAD_MAX_SIZE=5242880

# Expose the web container. In real production this is often 80 behind a host firewall or 127.0.0.1 behind another proxy.
WEB_PORT=8080
PUBLISHER_PORT=5174

# Frontend build args.
VITE_API_BASE_URL=/api
VITE_PUBLISHER_SITE_URL=https://pages.example.com
VITE_LOWCODE_HTTP_ALLOWED_ORIGINS=https://api.example.com

# Publisher build and runtime env.
PUBLISHER_API_BASE_URL=http://server:3000/api
PUBLISHER_SITE_URL=https://pages.example.com
PUBLISHER_REVALIDATE_SECRET=replace-this-with-an-independent-random-secret
PUBLISHER_REVALIDATE_TIMEOUT_MS=3000
PUBLISHER_LOWCODE_HTTP_ALLOWED_ORIGINS=https://api.example.com

# Optional AI page builder provider. Do not expose this key to the frontend.
AI_PROVIDER_BASE_URL=https://api.openai.com/v1
AI_PROVIDER_API_KEY=
AI_PROVIDER_MODEL=gpt-4.1-mini
AI_PROVIDER_TIMEOUT_MS=30000
AI_AGENT_MAX_STEPS=8
AI_AGENT_MAX_REPAIRS=1
AI_AGENT_TIMEOUT_MS=45000
AI_AGENT_MAX_CONTEXT_COMPONENTS=80
AI_AGENT_RATE_LIMIT_PER_MINUTE=12
