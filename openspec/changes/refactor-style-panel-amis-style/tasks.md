## 1. 外观面板结构

- [x] 1.1 梳理 `ComponentStyle.tsx` 当前表单、CSS 源码、恢复默认和搜索过滤逻辑，确认不改变 `stylesSetter`、store action 和 schema 契约。
- [x] 1.2 将外观页签整理为“快捷样式”和“CSS 源码”两个稳定分组，保持默认展开状态和搜索关键字过滤。
- [x] 1.3 在快捷样式分组标题展示过滤后的字段数量，并提供不会触发展开折叠误操作的“恢复默认”入口。
- [x] 1.4 为无 setter 或搜索无匹配时补充明确空状态，避免外观面板出现空白区域。

## 2. 快捷样式编辑

- [x] 2.1 统一尺寸类字段识别范围，确保宽度、高度、内外边距、字号、圆角和间距等字段使用数字输入加固定 `px` 后缀。
- [x] 2.2 保证数字尺寸写入 `styles` 时转换为 `px` 字符串，清空数字时从 `styles` 中移除对应字段。
- [x] 2.3 保证快捷样式修改后立即更新画布，并同步刷新 CSS 源码字符串。
- [x] 2.4 检查颜色、文本、选择器等非尺寸字段仍按原 setter 类型渲染和写入。

## 3. CSS 源码编辑

- [x] 3.1 优化 CSS 源码分组提示，明确只编辑 `.comp` 选择器内部声明并同步为内联样式。
- [x] 3.2 稳定 CSS 解析流程，将合法 CSS 声明转换为 React style key 后写入当前组件 `styles`。
- [x] 3.3 确保 CSS 源码变更同步回快捷样式表单，且不执行整表 reset、不导致 Monaco 焦点丢失。
- [x] 3.4 对无法解析或不支持的复杂 CSS 做安全处理，避免破坏当前组件树或写入不可表达结构。

## 4. 恢复默认与同步状态

- [x] 4.1 实现恢复默认样式时以 replace 模式清空当前组件 `styles`。
- [x] 4.2 恢复默认后同步重置快捷样式表单、CSS 源码和画布外观。
- [x] 4.3 确认恢复默认不会修改当前组件的 `props`、`children`、事件配置、id 或父子关系。
- [x] 4.4 保留成功反馈，并保持当前外观页签和当前选中组件不变。

## 5. 样式与视觉还原

- [x] 5.1 更新 `src/editor/settingPanel.css`，让外观页签接近参考截图的紧凑检查器风格：浅灰分组标题、细边框、12px 内容内边距、稳定输入高度。
- [x] 5.2 调整快捷样式输入控件、`px` 后缀、恢复默认按钮和提示块的间距、字号、颜色和 hover/focus 状态。
- [x] 5.3 调整 CSS 编辑器容器固定高度、边框、圆角和滚动行为，防止横向溢出或遮挡后续内容。
- [x] 5.4 检查右侧设置面板整体仍符合项目白底、细边框、蓝色功能强调和 6px 以内圆角体系。

## 6. 验证

- [x] 6.1 更新或补充编辑器回归测试，覆盖外观搜索、px 尺寸编辑、CSS 源码同步和恢复默认。
- [x] 6.2 运行 `npm run lint` 和 `npm run build`，确认 TypeScript 与样式改动可构建。
- [x] 6.3 如环境允许，运行 `npm run test:e2e:editor` 并检查右侧外观面板截图或浏览器实际效果。
- [x] 6.4 运行 OpenSpec 状态或校验命令，确认本 change 处于可 apply 状态。
