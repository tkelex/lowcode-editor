## 1. 共享协议与校验

- [x] 1.1 定义 AI 页面生成请求、页面意图、生成结果、warnings、assumptions 和校验错误的 shared TypeScript 类型。
- [x] 1.2 定义 AI 可用物料白名单和生成约束，覆盖 Page、布局、基础展示、表单、数据和反馈类常用物料。
- [x] 1.3 实现 AI 生成组件树校验工具，复用 `validateComponentTree` 并补充事件动作、安全规则和 custom JS 限制。
- [x] 1.4 实现生成结果规范化工具，补齐缺失 props、修正 parentId、保证 id 唯一且根 Page 合法。
- [x] 1.5 为 AI 校验和规范化工具补充单元测试，覆盖未知物料、非法父子关系、重复 id、custom action 和合法页面。

## 2. 后端 AI 网关

- [x] 2.1 新增后端 AI 模块、controller、service 和 DTO，提供页面生成接口。
- [x] 2.2 增加模型提供商配置和环境变量读取，确保模型 API key 只在服务端使用。
- [x] 2.3 在 AI 生成接口中复用项目/页面权限校验，限制 viewer 触发生成或写入类能力。
- [x] 2.4 实现模型请求封装，要求模型返回结构化 JSON，并处理超时、额度、网络和格式错误。
- [x] 2.5 增加 AI 生成审计日志或等效记录，记录项目、页面、用户、模型、耗时、状态和失败原因。
- [x] 2.6 增加后端错误码和 API 文档，说明 AI 不生成任意应用代码且不会暴露模型密钥。

## 3. 生成流程与 CRUD 协同

- [x] 3.1 实现自然语言到页面意图的 prompt 模板和输出解析。
- [x] 3.2 实现页面意图到低代码组件树的映射，优先使用现有 registry 支持的物料。
- [x] 3.3 实现接口说明/响应示例到字段映射的推断，生成表格列、表单项和详情字段建议。
- [x] 3.4 与 `add-data-model-crud-generation` 的数据源模型和 CRUD schema 生成器预留对接点。
- [x] 3.5 当 CRUD 生成器不可用时，返回明确 warning 并降级生成静态表格/表单草稿。
- [x] 3.6 实现一次受控修复流程：AI 输出校验失败时尝试修复，仍失败则返回可读错误。

## 4. 前端 AI 搭建体验

- [x] 4.1 新增 AI 页面搭建入口，放入编辑器中不遮挡核心画布操作的位置。
- [x] 4.2 实现 AI 输入面板，支持页面描述、接口说明、响应示例、目标范围和写入模式选择。
- [x] 4.3 实现生成中、取消、失败、warnings、assumptions 和空状态反馈。
- [x] 4.4 实现生成结果预览/摘要确认区，用户确认前不修改当前组件树。
- [x] 4.5 实现替换整页写入流程，确认后通过现有 store 写入组件树并保持可预览、可保存。
- [x] 4.6 实现插入当前容器流程，校验当前选中组件能接收子组件后再插入生成片段。
- [x] 4.7 对非法插入目标、非法生成结果和权限不足提供清晰错误反馈。

## 5. 保存、发布与安全边界

- [x] 5.1 确保 AI 生成页面继续走现有页面保存、版本和发布流程。
- [x] 5.2 确保公开发布页继续使用 `allowCustomJS=false`，AI 默认不得生成可执行 custom JS。
- [x] 5.3 确保 HTTP action、URL、headers、body 和 allowed origins 行为不因 AI 生成绕过现有限制。
- [x] 5.4 确保 AI 生成结果不会自动覆盖用户当前未确认编辑内容。

## 6. 验证与文档

- [x] 6.1 为 shared AI 类型、校验、规范化和生成辅助逻辑补充 `npm run test` 覆盖。
- [x] 6.2 为后端 AI 网关补充 build 验证和关键 API smoke 覆盖。
- [x] 6.3 添加编辑器 e2e，覆盖打开 AI 面板、生成、预览确认、替换整页和非法插入反馈。
- [x] 6.4 运行 `npm run lint`、`npm run build`、`npm run test` 和相关后端验证。
- [x] 6.5 更新 `docs/03-接口/接口说明.md`、架构文档和项目上下文索引，说明 AI 页面搭建边界、配置和安全约束。
- [x] 6.6 更新 AGENTS/协作上下文中与 AI 生成、schema 校验、验证命令相关的项目指导。
