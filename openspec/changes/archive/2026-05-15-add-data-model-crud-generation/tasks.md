## 1. 数据源模型契约与生成器

- [x] 1.1 定义前端数据源模型、接口配置、字段映射和 CRUD 生成选项的 shared TypeScript 类型。
- [x] 1.2 实现数据源模型校验工具，覆盖模型标识、接口 URL、请求方法、响应路径、字段 key、字段类型和重复字段。
- [x] 1.3 实现 CRUD schema 生成器，输入数据源模型后输出列表页、表单页和详情页组件树。
- [x] 1.4 为生成器补充单元测试，验证生成 schema 能通过现有迁移和组件树校验。

## 2. 后端配置存储与权限

- [x] 2.1 更新 Prisma schema，新增项目级数据源模型配置表，并生成 migration。
- [x] 2.2 新增 NestJS 数据源模型配置模块，支持列表、详情、创建、更新和删除。
- [x] 2.3 在配置接口中复用 ProjectAccessService，落实 owner/editor/viewer 权限边界。
- [x] 2.4 为数据源模型配置创建、更新和删除写入 AuditLog。
- [x] 2.5 更新后端 DTO、统一错误码和 API 文档，明确平台不托管业务记录数据。

## 3. 前端模型管理与生成向导

- [x] 3.1 新增 shared api 封装，覆盖数据源模型配置接口。
- [x] 3.2 在项目后台新增数据源模型管理入口和模型列表页面。
- [x] 3.3 实现数据源模型创建/编辑表单，支持接口配置、字段映射、展示位置和校验反馈。
- [x] 3.4 实现 CRUD 页面生成向导，支持选择数据源模型、页面类型、页面名称和路由。
- [x] 3.5 生成完成后创建普通 Page，并提供打开编辑器继续编辑的入口。

## 4. 编辑器、事件与运行时接入

- [x] 4.1 将生成页面的数据源、表格列、表单项和事件动作映射到现有物料 registry 支持的 props。
- [x] 4.2 确保生成表单的提交动作能调用外部新增/更新接口，并写入请求 body 模板。
- [x] 4.3 确保生成列表页能读取外部列表接口，并支持刷新、重新请求或明确的手动刷新反馈。
- [x] 4.4 补齐事件动作面板的能力过滤，避免生成页暴露无效组件方法或无目标动作。
- [x] 4.5 检查生成页面在编辑态、预览态和发布态的 dev/prod 表现一致性。

## 5. 验证与文档

- [x] 5.1 添加 shared 数据源模型/生成器测试，并运行 `npm run test`。
- [x] 5.2 添加或扩展 API smoke，覆盖数据源模型配置和项目权限边界。
- [x] 5.3 添加 e2e 覆盖创建数据源模型、生成 CRUD 页面、打开编辑器、预览和保存。
- [x] 5.4 运行 `npm run lint`、`npm run build` 和后端构建。
- [x] 5.5 更新 `docs/03-接口/接口说明.md`、架构文档和项目上下文索引，说明前端低代码平台定位和外部 API 数据边界。
