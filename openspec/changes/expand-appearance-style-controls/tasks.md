## 1. 样式元数据与通用目录

- [x] 1.1 扩展 `ComponentSetter` 的样式元数据类型约定，覆盖 `group`、`keywords`、`unit`、`control`、`min`、`max`、`step`、`placeholder` 等字段，同时保持旧 setter 写法兼容。
- [x] 1.2 在 `src/editor/registry/factory.ts` 增加样式 setter 工厂函数、分组选项和常用枚举选项。
- [x] 1.3 将 `commonStyleSetters` 从宽度/高度/外边距/内边距扩展为布局、尺寸、间距、文字、背景、边框、效果和显示状态的通用样式目录。
- [x] 1.4 检查各物料 registry 配置，移除与扩展通用目录重复的样式字段，并为 Text、Image、容器类或其他必要物料补充专属样式字段。

## 2. 外观面板分组呈现

- [x] 2.1 重构 `ComponentStyle.tsx` 中 setter 过滤逻辑，按样式分组生成快捷样式二级分组数据。
- [x] 2.2 在“快捷样式”主分组内渲染布局、尺寸、间距、文字、背景、边框、效果等二级分组，并在分组标题显示当前可见字段数量。
- [x] 2.3 设置高频分组默认展开、低频分组可折叠，保证切换组件和搜索时面板状态稳定。
- [x] 2.4 扩展外观搜索匹配范围，支持字段名、中文标签、分组名和关键词，并隐藏无匹配字段的分组。
- [x] 2.5 保持搜索空状态、无 setter 空状态和 CSS 源码分组显示规则不修改组件数据。

## 3. 样式控件与值规范化

- [x] 3.1 根据 setter 元数据渲染合适控件：px 数字输入、无单位数字输入、颜色输入、枚举选择和普通文本输入。
- [x] 3.2 将样式值规范化逻辑改为优先读取 `unit` 和 `control` 元数据，并保留现有字段名回退规则。
- [x] 3.3 确保清空任意外观字段时从当前组件 `styles` 中删除对应 key，而不是写入空字符串、`null` 或无效值。
- [x] 3.4 确保快捷样式修改后立即同步到画布和 CSS 源码，且不会导致表单或 Monaco 编辑器丢失焦点。
- [x] 3.5 确保 CSS 源码输入的合法声明继续写入 `styles`，并同步回已声明 setter 的可视化字段。

## 4. 真实控件样式落点

- [x] 4.1 扩展 `src/editor/materials/styleSplit.ts` 的样式分类，区分 shell-only、control-only 和 shell/control 双写样式。
- [x] 4.2 检查 Button 物料，确保字体、颜色、背景、边框、圆角、内边距、宽高等样式作用到真实 Ant Design Button。
- [x] 4.3 检查 Input、Select、Textarea、DatePicker 等表单控件物料，确保扩展视觉样式作用到真实控件本体。
- [x] 4.4 检查容器、文本、图片等非单控件物料，确保扩展样式应用到正确根节点或真实视觉元素。
- [x] 4.5 验证外边距、定位等 shell-only 样式不会造成真实控件与编辑器选中框错位。

## 5. 面板视觉与交互细节

- [x] 5.1 更新 `src/editor/settingPanel.css`，为外观二级分组、字段计数、颜色控件、枚举控件和紧凑表单间距提供稳定样式。
- [x] 5.2 确保右侧面板在常见宽度下没有横向溢出、文字重叠、按钮撑开或输入框跳动。
- [x] 5.3 确保恢复默认入口能清空扩展字段对应的所有自定义样式，并同步重置表单、CSS 源码和画布。
- [x] 5.4 保持外观页签与现有属性、事件页签的视觉语言一致，不引入 amis 依赖或新的全局 UI 风格。

## 6. 验证

- [x] 6.1 更新或补充编辑器回归测试，覆盖新增样式字段、分组搜索、字段清空、恢复默认和 CSS 源码同步。
- [x] 6.2 补充代表性真实控件验证，覆盖 Button、Input、Select 等物料的视觉样式落点。
- [x] 6.3 运行 `npm run lint` 和 `npm run build`，确认 TypeScript 与样式改动可构建。
- [x] 6.4 如环境允许，运行 `npm run test:e2e:editor` 并检查外观面板实际浏览器效果。
- [x] 6.5 运行 OpenSpec 状态或校验命令，确认 `expand-appearance-style-controls` 处于可 apply 状态。
