## 1. 事件面板交互

- [x] 1.1 增加事件页签内部筛选，支持全部、已配置、未配置和事件类别过滤，并保留现有关键词搜索。
- [x] 1.2 将事件数据展示为可复制 token，包含无 schema 时的 `event.args` 兜底提示。
- [x] 1.3 优化事件空状态和快捷添加动作入口，确保快捷动作仍受 `allowedActions` 约束。
- [x] 1.4 强化动作流水线展示，保留复制、编辑、删除、启用/禁用和上下移动能力。

## 2. 动作弹窗与样式

- [x] 2.1 支持从快捷入口预选动作类型，同时保持编辑已有动作时的初始化逻辑。
- [x] 2.2 更新事件面板样式，保证右侧窄面板内文本不横向溢出，操作按钮保持紧凑可扫读。

## 3. 文档同步

- [x] 3.1 同步事件动作规划文档，记录事件面板 UI 模型和组件事件策略。
- [x] 3.2 同步事件能力矩阵文档，确保当前 registry 已落地事件、事件数据和允许动作策略可追踪。

## 4. 验证

- [x] 4.1 运行相关静态检查或构建，确认 TypeScript 与样式改动可通过。
- [x] 4.2 检查 OpenSpec 状态，确认任务和规格处于可归档前状态。
