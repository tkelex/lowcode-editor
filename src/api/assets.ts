/**
 * @deprecated Use `src/shared/api/assets` instead. This compatibility file keeps old imports working.
 */
export {
  deleteProjectAsset,
  listProjectAssets,
  uploadProjectAsset,
} from '../shared/api/assets';
