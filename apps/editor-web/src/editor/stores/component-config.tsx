/**
 * @deprecated Use `src/editor/registry/component-config` instead. This compatibility file keeps old imports working.
 */
export {
  useComponentConfigStore,
} from '../registry/component-config';

export type {
  ComponentCategory,
  ComponentConfig,
  ComponentEvent,
  ComponentMethod,
  ComponentSetter,
  StyleSetterControl,
  StyleSetterUnit,
} from '../registry/component-config';
