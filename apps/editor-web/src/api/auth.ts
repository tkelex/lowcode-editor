/**
 * @deprecated Use `src/shared/api/auth` instead. This compatibility file keeps old imports working.
 */
export {
  getCurrentUser,
  getStoredToken,
  login,
  logout,
  register,
} from '../shared/api/auth';
