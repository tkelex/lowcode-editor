/**
 * @deprecated Use `src/shared/api/types` instead. This compatibility file keeps old imports working.
 */
export type {
  AuthResponse,
  AuditLog,
  EditorPage,
  PageSchema,
  PageVersion,
  Project,
  ProjectMember,
  ProjectRole,
  PublishedPage,
  User,
} from '../shared/api/types';
