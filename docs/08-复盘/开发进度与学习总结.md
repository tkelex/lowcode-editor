# 当前开发进度与学习总结

## 1. GitHub 连接

当前项目已添加远程仓库：

```bash
origin  https://github.com/tkelex/lowcode-editor.git
```

当前本地分支是：

```bash
master
```

远程默认分支查询时网络连接被 reset，所以暂时还没有 push。

如果确认要推送，可以执行：

```bash
git push -u origin master
```

如果远程默认分支是 `main`，可以先改名再推送：

```bash
git branch -M main
git push -u origin main
```

后续不会自动 push，除非明确要求。

补充说明：现在本地 `master` 已经成功关联并推送过 GitHub，后续新增 commit 仍然只会在你明确要求时 push。

## 2. 当前本地提交记录

目前已经按重要板块完成多个本地 commit，核心包括：

```bash
c16e19a docs: record save loop verification summary
993a3cd server: add initial Prisma migration
7f12216 chore: add docker postgres local setup
5cde745 docs: add development progress learning summary
da9e2e9 frontend: add auth dashboard and schema save flow
2c67205 server: add auth and page schema APIs
8b30dc7 docs: add production planning and project guidelines
```

## 3. 提交说明

### 3.1 文档和规范

提交：

```bash
8b30dc7 docs: add production planning and project guidelines
```

包含：

- `CLAUDE.md`
- `docs/02-架构/后端与数据库选型.md`
- `docs/01-产品/需求文档.md`
- `docs/99-归档/历史实施计划.md`
- `docs/05-开发/编码规范.md`
- `docs/07-安全/安全规范.md`
- `docs/05-开发/Git工作流.md`
- `docs/05-开发/本地开发指南.md`
- `.gitignore`

作用：

- 明确项目目标
- 明确技术选型
- 明确产品需求
- 明确实施计划
- 明确编码规范
- 明确安全规范
- 明确 Git 工作流

### 3.2 后端板块

提交：

```bash
2c67205 server: add auth and page schema APIs
```

包含：

- `server/`
- NestJS 后端项目
- Prisma 数据模型
- 用户注册 / 登录
- JWT 鉴权
- 项目 CRUD
- 页面 CRUD
- 页面 schema 保存 / 读取

核心文件：

```text
server/prisma/schema.prisma
server/src/main.ts
server/src/app.module.ts
server/src/modules/auth/
server/src/modules/projects/
server/src/modules/pages/
server/src/prisma/
server/src/common/
```

### 3.3 前端接入板块

提交：

```bash
da9e2e9 frontend: add auth dashboard and schema save flow
```

包含：

- 登录 / 注册页面
- 项目列表页面
- 页面列表页面
- 前端 API 请求层
- 编辑器打开页面时加载后端 schema
- 编辑器 Header 增加保存按钮
- 保存当前组件树到后端
- 修复前端 lint / build 配置

核心文件：

```text
src/App.tsx
src/api/http.ts
src/api/auth.ts
src/api/projects.ts
src/api/pages.ts
src/api/types.ts
src/features/auth/AuthView.tsx
src/features/projects/ProjectDashboard.tsx
src/editor/components/Header/index.tsx
src/editor/stores/components.tsx
```

## 4. 本次新增能力

当前项目已经从纯前端学习 Demo，开始转向具备产品闭环的低代码平台雏形。

本次新增能力包括：

1. 项目有了 Git 管理。
2. 项目连接到了 GitHub remote。
3. 有了产品需求文档。
4. 有了后端技术选型文档。
5. 有了项目实施计划。
6. 有了安全规范和编码规范。
7. 有了 NestJS 后端。
8. 有了 PostgreSQL / Prisma 数据模型。
9. 有了登录注册能力。
10. 有了项目 / 页面管理能力。
11. 有了前端保存 schema 到后端的闭环。
12. 有了 Docker Compose 本地 PostgreSQL。
13. 有了 Prisma migration。
14. 有了上下文索引文档和 `/context-index` skill。

## 5. 已验证内容

已通过以下命令：

```bash
npm run build
npm run lint
npm run prisma:generate --prefix server
npm run build --prefix server
npm run prisma:migrate --prefix server -- --name init
```

说明：

- 前端 TypeScript 构建通过。
- 前端 lint 通过。
- Prisma Client 生成通过。
- Prisma migration 执行通过。
- NestJS 后端构建通过。
- API 保存闭环 smoke test 通过。

## 6. 尚未完成的验证

后端 API 保存闭环已经通过，浏览器端完整人工验证还未完成。

后续需要在浏览器验证：

1. 启动 PostgreSQL。
2. 启动后端。
3. 启动前端。
4. 注册 / 登录。
5. 创建项目。
6. 创建页面。
7. 打开编辑器。
8. 拖拽组件并修改配置。
9. 点击保存。
10. 刷新页面。
11. 重新打开页面并确认组件树恢复。

完整验证路径：

```text
注册 / 登录
  ↓
创建项目
  ↓
创建页面
  ↓
打开编辑器
  ↓
拖拽组件并修改配置
  ↓
点击保存
  ↓
刷新页面
  ↓
重新打开页面并确认组件树恢复
```

## 7. 学习重点

你的当前背景是：主要熟悉 React 前端，对后端、数据库、安全和 Git 工程流程还不熟悉。

因此接下来建议重点学习以下内容。

### 7.1 产品和业务视角

建议先看：

```text
docs/01-产品/需求文档.md
docs/99-归档/历史实施计划.md
```

重点理解：

- 为什么低代码编辑器最重要的是“保存、再次打开、发布、回滚”。
- 为什么不是一开始堆组件数量。
- 为什么需要项目、页面、schema、版本这些业务概念。
- 什么叫“产品闭环”。

这部分可以帮助你从“写功能”转向“理解业务场景”。

### 7.2 后端最小业务架构

建议重点看：

```text
server/src/modules/auth/
server/src/modules/projects/
server/src/modules/pages/
```

NestJS 的基本分层：

```text
Controller：接收请求
Service：写业务逻辑
DTO：校验前端传来的数据
Guard：控制接口是否需要登录
Prisma：访问数据库
```

可以类比前端理解：

```text
页面组件        ≈ Controller
业务 hooks      ≈ Service
表单校验规则    ≈ DTO
路由鉴权        ≈ Guard
请求 API 层     ≈ Prisma / 数据库访问层
```

### 7.3 数据库模型

建议重点看：

```text
server/prisma/schema.prisma
server/prisma/migrations/20260428084614_init/migration.sql
```

目前有三个核心模型：

```text
User     用户
Project  项目
Page     页面
```

其中低代码编辑器最核心的是：

```prisma
schema Json
```

它保存的就是前端 Zustand 里的组件树。

核心闭环是：

```text
前端 components 树
        ↓ 保存
后端 Page.schema
        ↓ 读取
前端重新恢复 components 树
```

这就是第一阶段最重要的业务闭环。

### 7.4 前端如何从 Demo 变成产品

建议重点看：

```text
src/App.tsx
src/api/
src/features/
src/editor/components/Header/index.tsx
src/editor/stores/components.tsx
```

以前的流程是：

```text
打开页面 → Zustand 本地状态 → 编辑 / 预览
```

现在的流程是：

```text
登录
  ↓
项目列表
  ↓
页面列表
  ↓
从后端加载 schema
  ↓
写入 Zustand
  ↓
编辑
  ↓
保存到后端
```

这就是从前端 Demo 走向完整产品的第一步。

### 7.5 上下文索引和 token 管理

建议重点看：

```text
.claude/skills/context-index/SKILL.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/02-架构/架构说明.md
docs/03-接口/接口说明.md
docs/02-架构/技术决策记录.md
```

你可以这样理解上下文索引：

```text
完整源码        = 信息最多，但 token 成本最高
上下文索引文档  = 信息较少，但足够帮助 AI 定位
按需读取源码    = 需要改哪里，再读哪里
```

这个机制的价值是：

- 新会话不用重新扫全项目。
- 上下文压缩后可以快速恢复项目状态。
- 其它 AI 不支持 Claude Code Skill，也可以直接读 Markdown。
- `CLAUDE.md` 不需要塞太多内容，避免每次会话都浪费 token。

## 8. 后续协作规则

后续每完成一个重要板块，都按以下流程执行：

1. 完成代码。
2. 运行必要验证。
3. 创建一次 git commit。
4. 把总结写入项目文档。
5. 说明本次修改了哪些文件。
6. 说明为什么这样改。
7. 总结你应该学习的知识点。

## 9. 2026-04-28 保存闭环验证记录

本次完成的是第一阶段最关键的后端保存闭环验证：把前端页面 schema 真正写入 PostgreSQL，并再次读取确认数据一致。

### 9.1 本次提交

提交：

```bash
993a3cd server: add initial Prisma migration
```

包含：

```text
server/prisma/migrations/20260428084614_init/migration.sql
server/prisma/migrations/migration_lock.toml
```

作用：

- 把 `server/prisma/schema.prisma` 里的模型转换成数据库建表 SQL。
- 创建 `User`、`Project`、`Page` 三张核心业务表。
- 创建唯一索引和外键约束。
- 让其他环境可以通过 migration 复现相同的数据库结构。

### 9.2 本次验证通过的内容

PostgreSQL Docker 容器已启动并通过健康检查。

Prisma migration 已成功执行：

```bash
npm run prisma:migrate --prefix server -- --name init
```

前后端构建验证通过：

```bash
npm run build
npm run build --prefix server
```

API smoke test 已验证完整闭环：

```text
注册用户
  ↓
登录获取 token
  ↓
调用 /auth/me 验证登录态
  ↓
创建项目
  ↓
创建页面
  ↓
PATCH /api/pages/:id 保存 schema
  ↓
GET /api/pages/:id 读取 schema
  ↓
确认保存的 Button 组件仍然存在
```

验证结果摘要：

```json
{
  "ok": true,
  "userId": 1,
  "projectId": 1,
  "pageId": 1,
  "savedComponent": "Button"
}
```

### 9.3 本次遇到的问题

第一次运行 Prisma migration 时，命令停在交互式输入：

```text
Enter a name for the new migration
```

原因是 `prisma migrate dev` 在首次生成 migration 时需要 migration 名称。

处理方式是改成非交互命令：

```bash
npm run prisma:migrate --prefix server -- --name init
```

中断第一次命令后，PostgreSQL 里残留了一个 Prisma advisory lock，导致下一次迁移短暂超时。

处理方式是终止本地开发库里卡住的 Prisma migration 会话，然后重新运行 migration。

### 9.4 你应该重点学习的点

#### Prisma migration 是什么

`schema.prisma` 是数据库模型设计文件，migration 是 Prisma 根据模型生成的真实 SQL 变更记录。

可以这样理解：

```text
schema.prisma       = 设计图
migration.sql       = 真正施工的 SQL
PostgreSQL tables   = 施工完成后的数据库结构
```

所以 migration 文件需要提交到 Git，否则别人或部署环境不知道应该如何创建数据库表。

#### 为什么低代码 schema 用 JSONB

低代码页面的组件树结构不固定，不同页面可能有不同组件、属性、样式和事件配置。

如果每个组件属性都拆成数据库表，会非常复杂；当前阶段更适合把整棵组件树保存到 `Page.schema`：

```prisma
schema Json
```

在 PostgreSQL 里它会变成 `JSONB` 字段，既能保存灵活 JSON，也比普通文本更适合后续查询和扩展。

#### 保存闭环的核心价值

现在项目已经不只是前端 demo，而是具备了最小产品闭环：

```text
用户登录
  ↓
创建项目和页面
  ↓
编辑器生成组件树
  ↓
保存到数据库
  ↓
重新打开页面恢复组件树
```

这是低代码平台能成为产品的基础。后续的版本管理、发布、权限、部署，都是建立在这个闭环之上。

### 9.5 后续总结规则

从现在开始，每次完成重要代码块后，除了简短聊天说明，还要把以下内容写入项目文档：

1. 本次提交。
2. 修改了哪些文件。
3. 每个文件的作用。
4. 执行了哪些验证。
5. 遇到了什么问题以及如何解决。
6. 你应该学习的知识点。

优先更新：

```text
docs/08-复盘/开发进度与学习总结.md
```

如果某次内容属于专门主题，例如后端、本地开发、部署、安全，再补充到对应专题文档。

## 10. 2026-04-28 上下文索引 Skill 与文档记录

本次完成的是为项目建立“低 token 上下文入口”，解决上下文太多、压缩后恢复困难、其它 AI 接手项目成本高的问题。

### 10.1 本次新增文件

```text
.claude/skills/context-index/SKILL.md
docs/00-总览/项目上下文索引.md
docs/02-架构/架构说明.md
docs/03-接口/接口说明.md
docs/02-架构/技术决策记录.md
.claude/context/FILE_MAP.md
```

同时更新：

```text
CLAUDE.md
docs/08-复盘/开发进度与学习总结.md
```

### 10.2 每个文件的作用

`/.claude/skills/context-index/SKILL.md`：定义 `/context-index` skill 的目标、扫描步骤、输出文件和注意事项。

`docs/00-总览/项目上下文索引.md`：未来会话和其它 AI 的第一阅读入口，包含项目一句话说明、技术栈、推荐阅读顺序、核心闭环、命令和风险提醒。

`docs/02-架构/架构说明.md`：说明前端、后端、数据库之间如何协作，以及编辑器打开页面和保存页面的完整数据流。

`docs/03-接口/接口说明.md`：集中记录后端接口、鉴权方式、请求/响应结构、schema contract 和 API smoke test 路径。

`docs/02-架构/技术决策记录.md`：记录产品和技术决策，例如为什么第一阶段先做保存闭环、为什么用 NestJS/PostgreSQL/Prisma、为什么做上下文索引而不是 MCP。

`.claude/context/FILE_MAP.md`：关键文件地图，说明哪些文件负责什么、修改风险是什么、常见修改路线怎么走。

`CLAUDE.md`：新增 Context Index 入口，避免把所有项目细节塞进每次都会加载的上下文。

### 10.3 为什么这样做

上下文索引的目标不是替代源码，而是帮助 AI 更快判断“接下来该读哪些源码”。

推荐流程是：

```text
先读 CLAUDE.md
  ↓
读 docs/00-总览/项目上下文索引.md
  ↓
读 .claude/context/FILE_MAP.md
  ↓
根据任务按需读源码
```

这样比每次重新扫描全项目更省 token，也更适合上下文压缩后的恢复。

### 10.4 你应该学习的点

#### CLAUDE.md 不应该无限变大

`CLAUDE.md` 每次 Claude Code 会话都会读，如果里面放太多细节，会持续浪费 token。

更好的方式是：

```text
CLAUDE.md = 短入口 + 必须遵守的规则
索引文档 = 可按需读取的项目说明
源码     = 真正权威的信息
```

#### Skill 和 Markdown 的分工

Skill 适合定义“如何生成上下文索引”的流程。

Markdown 适合保存“当前项目是什么样”的结果。

其它 AI 可能不能运行 Claude Code Skill，但一般都可以读取 Markdown，所以项目知识应该落到文档里。

#### MCP 不是当前阶段的首选

MCP 适合连接外部系统或做更复杂的跨工具能力，例如数据库管理、部署平台、issue 系统、向量检索等。

当前项目只需要让 AI 更好理解本仓库，所以先用 Skill + 文档索引更轻量。

## 11. 2026-04-28 多页面创建修复与回滚功能现状

### 11.1 用户发现的问题

在浏览器手测时发现：同一个项目里创建第一个页面成功，但创建第二个页面会报错。

同时，当前编辑器界面只有“保存”按钮，没有“回滚”入口。

### 11.2 问题原因

创建页面弹窗里，页面路径默认值一直是：

```text
/home
```

数据库对页面路径有唯一约束：

```prisma
@@unique([projectId, routePath])
```

这表示同一个项目下不能有两个相同的 `routePath`。

所以第二次创建页面时，如果用户没有手动改路径，仍然提交 `/home`，后端会触发唯一约束错误。

### 11.3 本次修复

前端：

```text
src/features/projects/ProjectDashboard.tsx
```

修改点：

- 新增页面表单实例 `pageForm`。
- 点击“新建页面”时重置表单。
- 根据已有页面路径生成不重复的默认路径：

```text
/page-1
/page-2
/page-3
```

这样用户创建第二个页面时，不会默认继续使用 `/home`。

后端：

```text
server/src/modules/pages/pages.service.ts
```

修改点：

- 捕获 Prisma 唯一约束错误 `P2002`。
- 返回更明确的 `409 Conflict`：

```text
Page route path already exists in this project
```

这样即使用户手动输入了重复路径，后端也会返回更容易理解的错误。

### 11.4 已验证内容

已运行：

```bash
npm run build
npm run build --prefix server
```

并通过 API 验证：同一个项目下可以创建两个不同路径的页面：

```json
{
  "ok": true,
  "pageRoutes": [
    "/page-1",
    "/page-2"
  ]
}
```

### 11.5 关于回滚功能

你没有看到回滚入口是正常的，因为当前阶段还没有实现页面版本管理和回滚。

目前已经实现的是：

```text
保存当前页面 schema
```

还没有实现：

```text
保存历史版本
查看版本列表
选择某个版本回滚
```

下一阶段建议做“页面版本管理”，核心模型会是：

```text
PageVersion
```

大致流程：

```text
每次点击保存
  ↓
保存当前 Page.schema
  ↓
同时生成一条 PageVersion
  ↓
版本列表里展示历史记录
  ↓
点击回滚
  ↓
把某个 PageVersion.schema 恢复到 Page.schema
```

这是低代码平台从“能保存”走向“可放心使用”的关键能力。

### 11.6 你应该学习的点

#### 唯一约束是什么

数据库唯一约束用来保证某些字段组合不能重复。

当前项目里：

```prisma
@@unique([projectId, routePath])
```

意思是：

```text
同一个项目下，页面路径不能重复。
不同项目下，可以都有 /home。
```

这就是业务规则落到数据库层的例子。

#### 前端默认值也属于产品体验

这次 bug 不是后端不能创建第二个页面，而是前端默认值让用户很容易提交重复路径。

所以产品化开发里，表单默认值、错误提示、校验规则都很重要。

#### 保存不等于回滚

保存只代表覆盖当前最新状态。

回滚需要先有历史版本记录，所以必须新增版本表或版本存储机制。


## 12. 2026-04-28 预览模式模块不可见修复

### 12.1 用户发现的问题

点击编辑器顶部“预览”后，页面里看不到对应模块，用户会以为预览没有渲染。

### 12.2 问题原因

预览模式使用的是物料的 `prod.tsx`，不是编辑模式的 `dev.tsx`。

编辑模式下：

```text
src/editor/components/EditArea/index.tsx
  ↓
componentConfig[component.name].dev
```

预览模式下：

```text
src/editor/components/Preivew/index.tsx
  ↓
componentConfig[component.name].prod
```

部分布局组件在编辑态有边框、最小高度和拖拽提示，但在预览态更接近真实页面，没有明显边框。

例如空 `Container` 在预览态没有内容时，即使已经渲染，也可能看起来像什么都没有。

另外，如果某个组件缺少 `prod` 注册，原先预览会直接返回 `null`，用户看不到任何提示。

### 12.3 本次修复

修改：

```text
src/editor/materials/Page/prod.tsx
src/editor/materials/Container/prod.tsx
src/editor/components/Preivew/index.tsx
```

具体改动：

- `Page` 预览态增加最小高度和白色背景。
- `Container` 预览态增加最小高度和虚线边框，让空容器也可见。
- `Preview` 外层增加背景色。
- 如果某个组件没有 `prod` 预览组件，显示“未找到 xxx 的预览组件”提示，而不是静默空白。

### 12.4 已验证内容

已运行：

```bash
npm run build
npm run build --prefix server
```

说明：

- 前端 TypeScript 和生产构建通过。
- 后端构建通过。

### 12.5 你应该学习的点

#### 低代码编辑器通常有两套渲染

编辑器里常见两套组件：

```text
dev  = 编辑态组件，用于拖拽、选中、hover、辅助边框
prod = 运行态组件，用于真实预览或发布页面
```

所以一个组件在编辑态可见，不代表预览态一定可见；预览态要看 `prod.tsx` 是否正确渲染。

#### 空容器需要可视化提示

低代码编辑器里，容器类组件即使没有子节点，也应该让用户知道它存在。

否则用户会误以为：

```text
组件没有保存
组件没有渲染
预览坏了
```

这属于产品体验问题，不只是代码问题。

#### 静默失败不适合编辑器

原来缺少 `prod` 时直接返回 `null`，这对开发者和用户都不友好。

更好的方式是显示明确占位：

```text
未找到 Button 的预览组件
```

这样问题可以被快速定位。

## 13. 2026-04-28 页面版本历史与回滚功能

本次完成的是页面版本管理 MVP：每次保存页面时自动生成历史版本，用户可以在编辑器里打开版本历史，并选择某个版本回滚。

### 13.1 本次新增能力

```text
点击保存
  ↓
更新 Page.schema 当前页面
  ↓
创建 PageVersion 历史版本
  ↓
打开版本历史
  ↓
选择版本并确认回滚
  ↓
恢复旧 schema 到 Page.schema
  ↓
再创建一条 rollback 来源的新版本
```

这让编辑器从“只能覆盖保存”变成“可以恢复历史状态”。

### 13.2 本次修改文件

数据库：

```text
server/prisma/schema.prisma
server/prisma/migrations/20260428120657_add_page_versions/migration.sql
```

作用：

- 新增 `PageVersion` 模型和数据表。
- `PageVersion.schema` 保存某一次保存或回滚时的完整页面 schema。
- `versionNo` 记录页面内递增版本号。
- `source` 区分版本来源：`save` 或 `rollback`。
- 页面删除时，历史版本跟随 cascade 删除。

后端：

```text
server/src/modules/pages/pages.controller.ts
server/src/modules/pages/pages.service.ts
server/src/modules/pages/dto/rollback-page.dto.ts
```

作用：

- 保存页面时同时创建历史版本。
- 新增 `GET /api/pages/:id/versions` 查询版本列表。
- 新增 `POST /api/pages/:id/rollback` 回滚到指定版本。
- 回滚前校验页面归属和版本归属，避免跨用户或跨页面回滚。
- 回滚后再创建一条 `source = rollback` 的版本，保留操作记录。

前端：

```text
src/api/pages.ts
src/api/types.ts
src/editor/components/Header/index.tsx
```

作用：

- 新增 `PageVersion` 类型。
- 新增版本列表和回滚 API 请求方法。
- 编辑器 Header 增加“版本历史”按钮。
- 使用 Drawer 展示版本列表。
- 使用 Popconfirm 防止误点回滚。
- 回滚成功后调用 `setComponents`，让编辑器画布立即恢复旧组件树。

文档：

```text
docs/03-接口/接口说明.md
docs/02-架构/架构说明.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/08-复盘/开发进度与学习总结.md
```

作用：

- 同步记录版本接口、架构数据流、上下文索引和关键文件地图。

### 13.3 已验证内容

已运行：

```bash
npm run prisma:migrate --prefix server -- --name add_page_versions
npm run prisma:generate --prefix server
npm run build
npm run build --prefix server
```

API smoke test 已验证完整版本回滚闭环：

```text
注册用户
  ↓
创建项目
  ↓
创建页面
  ↓
第一次保存 schema，生成 v1
  ↓
第二次保存不同 schema，生成 v2
  ↓
查询版本列表
  ↓
回滚到 v1
  ↓
读取页面，确认 schema 恢复
  ↓
再次查询版本列表，确认新增 rollback 版本
```

验证结果摘要：

```json
{
  "ok": true,
  "pageId": 12,
  "versionCount": 3,
  "restoredText": "版本一",
  "rollbackVersionNo": 3
}
```

说明：

- 保存会生成 `source = save` 的版本。
- 回滚会恢复旧 schema。
- 回滚行为本身会生成 `source = rollback` 的新版本。
- 前端和后端生产构建均通过。

### 13.4 本次遇到的问题

#### Prisma Client 没有及时更新

新增 `PageVersion` 后，第一次后端构建报错：

```text
Property 'pageVersion' does not exist on type 'PrismaService'
```

原因是修改了 `schema.prisma`，但 Prisma Client 还没有重新生成。

处理方式：

```bash
npm run prisma:generate --prefix server
```

#### Windows 下 Prisma query engine DLL 被占用

第一次执行 Prisma generate 时遇到 DLL rename 权限错误。

原因是本地 Nest dev server 或残留 Prisma migration 进程占用了 Prisma query engine 文件。

处理方式：

- 停止后端 dev server 和残留 Prisma 进程。
- 保留前端 Vite 进程。
- 重新执行 Prisma generate。

#### 进度文档不能整文件覆盖

记录本次总结时曾误把进度文档改成简短版本，可能丢失之前的学习记录。

处理方式：

```bash
git restore -- docs/08-复盘/开发进度与学习总结.md
```

恢复后改为只追加第 13 节。

### 13.5 你应该学习的点

#### 保存版本和当前页面为什么要分开

当前页面：

```text
Page.schema
```

表示用户现在打开编辑器看到的最新状态。

历史版本：

```text
PageVersion.schema
```

表示某一次保存或回滚时的快照。

这样设计的好处是：

- 读取当前页面很简单，只查 `Page`。
- 历史记录不会覆盖当前状态。
- 回滚只是把某个历史快照复制回当前页面。

#### 回滚不是删除历史

回滚不是把版本列表倒退，也不是删除后面的版本。

更合理的做法是：

```text
旧版本 v1
新版本 v2
用户回滚到 v1
系统生成 v3，内容等于 v1，source = rollback
```

这样历史是完整的，后续可以知道用户什么时候执行过回滚。

#### 权限校验不能只查 versionId

回滚接口不能只根据 `versionId` 查版本，否则可能出现用户拿到别人版本 ID 后跨页面回滚。

正确逻辑是：

```text
先确认 page 属于当前用户
再确认 version 属于这个 page
最后才允许回滚
```

这就是后端业务接口必须做 owner 校验的原因。

#### 版本号和并发

当前 MVP 使用：

```text
当前最大 versionNo + 1
```

这对单人编辑和本地开发足够。以后如果要做多人协作或高并发保存，需要考虑事务冲突、重试或更严格的版本生成策略。

#### 前端回滚后要更新 Zustand

后端回滚成功只代表数据库已经恢复。编辑器画布要立即变化，还必须把返回的 schema 写回前端状态：

```text
page.schema.components → useComponetsStore.setComponents
```

否则用户会以为回滚没有生效。

## 14. 2026-04-28 页面版本记录删除功能

本次补齐的是版本历史里的“删除版本记录”能力。它只删除某条 `PageVersion` 历史快照，不修改当前 `Page.schema`，也不会改变编辑器当前画布内容。

### 14.1 本次新增能力

```text
打开版本历史
  ↓
选择某条历史版本
  ↓
点击删除并二次确认
  ↓
DELETE /api/pages/:id/versions/:versionId
  ↓
后端校验页面 owner 和版本归属
  ↓
删除对应 PageVersion
  ↓
前端刷新版本列表
```

删除后该版本不能再用于回滚，但当前页面内容仍以 `Page.schema` 为准。

### 14.2 本次修改文件

后端：

```text
server/src/modules/pages/pages.controller.ts
server/src/modules/pages/pages.service.ts
```

作用：

- 新增 `DELETE /api/pages/:id/versions/:versionId`。
- 删除前复用页面 owner 校验，确保只能操作自己的页面。
- 删除时同时匹配 `pageId` 和 `versionId`，避免只靠版本 ID 操作错误页面。
- 删除不存在或不属于该页面的版本时返回 not found。

前端：

```text
src/api/pages.ts
src/editor/components/Header/index.tsx
```

作用：

- 新增 `deletePageVersion(pageId, versionId)` API 封装。
- 在版本历史 Drawer 每条记录旁增加“删除”按钮。
- 使用 `Popconfirm` 二次确认，提示删除后不可回滚。
- 删除成功后刷新版本列表，不调用 `setComponents`，因为当前画布不应该变化。

文档：

```text
docs/03-接口/接口说明.md
docs/02-架构/架构说明.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/08-复盘/开发进度与学习总结.md
```

作用：

- 同步记录删除版本接口、架构数据流、上下文索引、关键文件职责和本次学习总结。

### 14.3 已验证内容

已运行：

```bash
npm run build
npm run build --prefix server
```

说明：

- 前端 TypeScript 和生产构建通过。
- 后端 NestJS 构建通过。

API smoke test 已验证版本删除闭环：

```text
注册 / 登录
  ↓
创建项目和页面
  ↓
保存两个不同 schema，生成至少两个版本
  ↓
删除其中一个历史版本
  ↓
再次查询版本列表，确认该版本消失
  ↓
读取当前页面，确认 Page.schema 没有变化
  ↓
尝试回滚已删除版本，确认返回 not found
```

验证结果摘要：

```json
{
  "ok": true,
  "pageId": 13,
  "deletedVersionId": 8,
  "deletedVersionNo": 1,
  "versionsBefore": 2,
  "versionsAfter": 1,
  "currentText": "删除前版本二",
  "rollbackDeletedStatus": 404
}
```

说明：

- 删除版本后，版本列表数量从 2 变成 1。
- 被删除的版本不再出现在版本列表。
- 删除版本没有改变当前页面 schema，当前文本仍然是 `删除前版本二`。
- 回滚已删除版本返回 404。

### 14.4 你应该学习的点

#### 删除历史版本不等于回滚

低代码页面当前状态和历史快照是两种不同数据：

```text
Page.schema        = 当前页面状态
PageVersion.schema = 某个历史快照
```

删除历史版本只应该删除 `PageVersion`，不能改 `Page.schema`。否则用户只是想清理历史记录，却会意外改变当前页面。

#### 为什么删除时要同时匹配 pageId 和 versionId

`versionId` 虽然是唯一 ID，但接口语义是“删除某个页面下的某个版本”：

```text
DELETE /api/pages/:id/versions/:versionId
```

所以后端应该确认两件事：

```text
page 属于当前用户
version 属于这个 page
```

这能避免跨用户、跨页面误删。

#### 为什么用 deleteMany

Prisma 的 `delete` 通常按唯一键删除，虽然可以用 `id`，但不能在同一个 `where` 里表达 `pageId` 归属约束。

这里用：

```ts
deleteMany({ where: { id: versionId, pageId } })
```

可以同时满足“版本 ID 正确”和“版本属于当前页面”。如果 `count === 0`，就说明这个版本不存在或不属于该页面。

#### 前端删除成功后只刷新列表

回滚成功后要更新 Zustand，因为当前页面内容变了。

删除版本成功后不应该更新 Zustand，因为当前页面内容没变，只是历史列表变了。

所以前端只需要：

```text
await loadVersions()
```

而不是：

```text
setComponents(...)
```


## 16. 2026-05-06 物料区扩充与编辑器视觉优化

本次完成的是编辑器产品化体验升级：借鉴成熟低代码编辑器的形态，把左侧物料区从简单扁平列表升级为分类搜索面板，并补充一批常用基础物料，同时优化顶部栏、画布、设置面板和容器默认样式。

### 16.1 本次新增能力

```text
左侧物料区
  ↓
按布局 / 基础 / 表单 / 数据展示 / 反馈分类展示
  ↓
支持按组件名、中文描述和 keywords 搜索
  ↓
拖入 Text / Image / Divider / Card / Alert / Input / Select / Switch
  ↓
右侧属性面板修改基础 props 和 styles
  ↓
保存、预览、发布继续复用同一份组件树 schema
```

这让项目不再只是少量按钮/容器的学习 demo，而更接近可演示的低代码编辑器雏形。

### 16.2 本次修改文件

物料注册：

```text
src/editor/stores/component-config.tsx
```

作用：

- 给物料配置增加 `category`、`icon`、`keywords`、`sort` 元信息。
- 给已有 Container、Button、Modal、Table、Form 等物料补充分类信息。
- 注册 Text、Image、Divider、Card、Alert、Input、Select、Switch 八个新物料。

物料面板：

```text
src/editor/components/Material/index.tsx
src/editor/components/MaterialItem/index.tsx
src/editor/components/MaterialWrapper/index.tsx
```

作用：

- 增加物料搜索框。
- 按布局、基础、表单、数据展示、反馈分类展示。
- 物料项从黑色虚线块升级为带图标、中文名称和 hover 效果的卡片。
- 保留 React DnD 拖拽逻辑，`item.type` 仍使用物料 `name`。

新增物料：

```text
src/editor/materials/Text/*
src/editor/materials/Image/*
src/editor/materials/Divider/*
src/editor/materials/Card/*
src/editor/materials/Alert/*
src/editor/materials/Input/*
src/editor/materials/Select/*
src/editor/materials/Switch/*
```

作用：

- 每个物料都提供 `dev.tsx` 和 `prod.tsx`。
- `dev.tsx` 用于编辑态拖拽、选中和辅助视觉。
- `prod.tsx` 用于预览和公开发布运行态。
- Card 是容器型物料，可以继续拖入子组件。

编辑器视觉和设置面板：

```text
src/editor/index.tsx
src/editor/components/Header/index.tsx
src/editor/components/EditArea/index.tsx
src/editor/components/Setting/index.tsx
src/editor/components/Setting/ComponentAttr.tsx
src/editor/materials/Page/dev.tsx
src/editor/materials/Container/dev.tsx
```

作用：

- 顶部栏改成浅色背景、柔和边框和更清晰标题。
- 中间画布改成灰色工作区 + 白色页面纸张。
- 未选中组件时右侧设置面板展示空状态。
- Page、Container 默认边框和空状态提示更接近编辑器产品。
- ComponentAttr 支持 `inputNumber`，用于图片宽高等数字属性。
- Page、Container、Card 的 drop accept 列表支持新增物料。

文档：

```text
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/02-架构/架构说明.md
docs/06-测试与验收/功能演示流程.md
docs/08-复盘/开发进度与学习总结.md
```

作用：

- 更新当前项目能力描述。
- 更新物料系统和新增物料的文件地图。
- 更新架构文档中的 registry + dev/prod + 分类元信息说明。
- 更新演示流程，加入搜索物料、拖入 Card/Text/Image/Form 控件等步骤。

### 16.3 已验证内容

已运行：

```bash
npm run build
npm run build --prefix server
```

结果：

- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- 前端构建只有 Vite chunk size warning，不影响构建结果。

### 16.4 尚未完成的验证

仍建议进行浏览器手测：

- 启动 PostgreSQL、后端和前端。
- 登录并打开一个页面。
- 在物料区搜索 Text、Image、Input。
- 拖入 Text、Image、Card、Alert、Input、Select、Switch。
- 把 Text、Button、Input 拖入 Card 或 Container。
- 修改右侧属性和样式，确认画布实时变化。
- 点击保存后刷新或重新进入页面，确认组件树恢复。
- 点击预览，确认 prod 渲染正常。
- 点击发布并打开 `/publish/:publicId`，确认公开页能展示新增组件。

### 16.5 你应该学习的点

#### 物料注册表是低代码编辑器的扩展中心

新增物料不是只写一个 React 组件，还需要把它接入编辑器体系：

```text
dev 组件
prod 组件
默认 props
setter
分类元信息
拖拽接收规则
```

这样左侧物料区、画布渲染、右侧设置面板、预览和发布页才能一起工作。

#### dev/prod 分离能让编辑态和运行态各司其职

编辑态组件需要额外能力：

```text
data-component-id
React DnD drag/drop
hover/selected 辅助样式
空容器占位提示
```

运行态组件则应该更接近真实页面，不应该暴露编辑器辅助 UI。

#### 分类和搜索属于产品体验

物料数量变多后，如果仍然是扁平列表，用户很难找到组件。

所以物料注册表里增加：

```ts
category
icon
keywords
sort
```

可以让物料区自动按产品逻辑组织，而不是在 UI 里写死每个组件。

#### 容器接收规则容易漏改

Page、Container、Card 都是可放置子组件的容器。新增物料后，如果只注册到物料区，但忘记更新容器的 `useMaterailDrop` accept 列表，就会出现“左侧能拖，画布不能放”的问题。

#### 构建通过不等于浏览器体验完整通过

本次 `npm run build` 和 `npm run build --prefix server` 已通过，说明类型和生产构建没有阻断问题。

但拖拽、选中遮罩、右侧属性实时更新、保存恢复、公开发布页展示都属于浏览器交互链路，仍需要手测确认。


## 17. 2026-05-06 源码面板修复与常用物料事件绑定

本次修复了左侧「源码」面板只能展示、不能安全应用 JSON 修改的问题，并补齐常用物料的基础事件绑定能力。

### 17.1 本次新增和修复能力

```text
源码面板
  ↓
编辑组件树 JSON 草稿
  ↓
实时解析和轻量结构校验
  ↓
点击“应用源码”后写回 Zustand components
  ↓
非法 JSON 不会破坏当前画布
```

```text
选中常用物料
  ↓
右侧事件面板显示可绑定事件
  ↓
添加 / 编辑 / 删除事件动作
  ↓
预览或公开运行态触发 prod 组件事件
  ↓
执行访问链接、消息提示、组件方法等动作
```

### 17.2 本次修改文件

前端源码面板：

```text
src/editor/components/Source/index.tsx
```

作用：

- 增加 Monaco 本地编辑草稿。
- 增加 JSON 解析和组件树结构校验。
- 增加“应用源码”按钮。
- 非法 JSON 只显示错误，不写入组件树。

事件配置面板：

```text
src/editor/components/Setting/ComponentEvent.tsx
src/editor/components/Setting/ActionModal.tsx
```

作用：

- 编辑已有动作时记录对应事件，避免写到错误事件。
- 删除动作改为不可变更新，避免直接修改原数组。
- 新增动作时清理上一次编辑状态。
- 打开已有动作时初始化当前配置，直接确认不会丢配置。

物料注册和运行态：

```text
src/editor/stores/component-config.tsx
src/editor/components/Preivew/index.tsx
src/editor/materials/Container/prod.tsx
src/editor/materials/Text/prod.tsx
src/editor/materials/Image/prod.tsx
src/editor/materials/Divider/prod.tsx
src/editor/materials/Card/prod.tsx
src/editor/materials/Alert/prod.tsx
src/editor/materials/Input/prod.tsx
src/editor/materials/Select/prod.tsx
src/editor/materials/Switch/prod.tsx
```

作用：

- 给 Text、Image、Divider、Card、Alert、Input、Select、Switch、Container 补齐基础事件声明。
- prod 组件转发事件 props 到真实 DOM 或 Ant Design 组件。
- Preview 对未知组件和异常 actions 做保护，避免 schema 异常时预览崩溃。
- 公开发布页仍保持 `allowCustomJS={false}`，不执行用户自定义 JS。

文档：

```text
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/02-架构/架构说明.md
docs/06-测试与验收/功能演示流程.md
docs/08-复盘/开发进度与学习总结.md
```

作用：

- 同步当前能力：源码面板可应用 JSON 修改、常用物料支持事件绑定。
- 补充事件绑定链路说明。
- 更新演示流程中的事件配置步骤。

### 17.3 已验证内容

已运行：

```bash
npm run build
```

结果：

- 前端 TypeScript 和 Vite 生产构建通过。
- 构建仅有 Vite chunk size warning，不影响构建结果。

### 17.4 尚未完成的验证

仍建议进行浏览器手测：

- 打开「源码」tab，修改 Text 或 Button 的 props 文本。
- 输入非法 JSON，确认出现错误提示且画布不变。
- 改回合法 JSON，点击“应用源码”，确认画布更新。
- 给 Button/Text/Input/Switch 绑定“消息提示”动作。
- 编辑已有动作后不修改字段直接确认，确认动作没有丢失。
- 进入预览模式触发事件，确认动作执行。
- 发布页面后打开公开地址，确认普通事件可用，自定义 JS 不执行。

### 17.5 你应该学习的点

#### 源码面板不能直接边输入边改全局状态

JSON 编辑器里的输入过程经常会短暂处于非法状态，例如少一个逗号或括号。如果每次输入都直接写入 Zustand，画布很容易被破坏。

更安全的方式是：

```text
本地草稿
  ↓
解析和校验
  ↓
用户确认应用
  ↓
写入全局组件树
```

#### 事件绑定需要 registry、设置面板、运行态三段同时打通

只在右侧面板显示事件还不够。完整链路是：

```text
component-config.tsx 声明 events
  ↓
ComponentEvent 保存 actions
  ↓
Preview 注入事件 props
  ↓
prod 组件转发事件 props
```

其中任何一段漏掉，都会出现“能配置但不能触发”或“组件根本看不到事件项”的问题。

#### 公开页要区分普通事件和 customJS

普通事件动作如消息提示、跳转链接可以在公开页运行；但 `customJS` 是用户编写并执行的代码，公开页必须保持禁用，避免把未沙箱化的脚本执行能力暴露给访客页面。


## 18. 2026-05-07 P0 稳定性：schema 校验与撤销重做

本次把后续完善计划写入 `docs/01-产品/后续路线图.md`，并按 P0 优先级完成第一阶段稳定性建设：统一组件 schema 校验，以及基础撤销 / 重做。

### 18.1 本次新增能力

```text
源码面板 / 保存 / 发布
  ↓
统一校验组件树 schema
  ↓
合法才允许应用、保存或发布
```

```text
属性 / 样式 / 事件 / 拖拽 / 删除 / 源码应用
  ↓
写入组件 store 历史栈
  ↓
顶部栏撤销 / 重做
```

打开页面和版本回滚属于快照载入，会清空历史，不会让用户撤销到上一个页面或旧版本之前的状态。

### 18.2 本次修改文件

计划文档：

```text
docs/01-产品/后续路线图.md
```

作用：记录 P0-P6 后续完善计划，并标记当前已完成的 P0 第一阶段。

schema 校验：

```text
src/editor/schema/validateComponents.ts
src/editor/components/Source/index.tsx
src/editor/components/Header/index.tsx
docs/03-接口/接口说明.md
docs/02-架构/架构说明.md
```

作用：

- 新增统一组件树校验器。
- 校验根节点数组、Page 根节点、唯一 id、注册物料、props/styles、children、父子接收规则和已有 `parentId`。
- 源码应用、保存、发布前统一校验，避免非法 schema 进入持久化和发布流程。

撤销 / 重做：

```text
src/editor/stores/components.tsx
src/editor/hooks/useMaterailDrop.ts
src/editor/components/Header/index.tsx
src/App.tsx
```

作用：

- 组件 store 增加 `pastComponents` / `futureComponents` 历史栈。
- 新增 `undo`、`redo`、`moveComponent`。
- Header 增加撤销、重做按钮。
- 拖拽移动改为单次 `moveComponent`，避免一次移动产生删除和添加两段历史。
- 打开页面和版本回滚调用 `setComponents(..., { recordHistory: false })`，作为快照载入。

上下文文档：

```text
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
docs/02-架构/技术决策记录.md
```

作用：同步记录 schema 校验、撤销 / 重做和新的关键文件。

### 18.3 已验证内容

已运行：

```bash
npm.cmd run lint
npm.cmd run build
npm.cmd run build --prefix server
```

结果：

- 前端 lint 通过。
- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- 前端构建仍有 Vite chunk size warning，不影响本次结果。

### 18.4 尚未完成的验证

仍建议浏览器手测：

- 修改属性后点击撤销 / 重做，确认画布恢复。
- 修改样式后点击撤销 / 重做，确认 CSS 恢复。
- 添加、删除、拖拽移动组件后撤销 / 重做。
- 在源码面板修改 JSON 并应用，然后撤销。
- 打开页面或执行版本回滚后，确认撤销按钮从空历史开始。
- 手动构造非法源码，例如重复 id、未知组件名、把 TableColumn 放到 Page 下，确认源码应用、保存和发布都被拦截。

### 18.5 你应该学习的点

#### 校验要放在所有关键入口

只在源码面板校验还不够。保存和发布是更关键的出口，如果不校验，非法组件树仍然可能进入数据库或公开页面。

#### 撤销 / 重做要围绕业务状态建模

当前编辑器真正的业务状态是组件树，所以历史栈记录组件树快照，而不是 DOM、选中框位置或预览模式。这让撤销更稳定，也更符合用户直觉。

#### 载入快照和编辑修改要区分

打开页面、版本回滚是“换一份页面快照”，不应该让用户撤销回上一个页面或上一个版本操作之前。源码应用则是用户主动修改当前页面，应该可以撤销。


## 19. 2026-05-07 文档目录治理

本次整理的是 `docs` 目录结构。之前所有文档都平铺在 `docs` 下，正式文档、学习材料、面试稿和历史计划混在一起，后续维护成本较高。

### 19.1 本次整理结果

新增文档门户：

```text
docs/README.md
```

新的目录结构：

```text
docs/
  00-总览/
  01-产品/
  02-架构/
  03-接口/
  04-编辑器/
  05-开发/
  06-测试与验收/
  07-安全/
  08-复盘/
  09-学习资料/
  99-归档/
```

### 19.2 文档归类原则

- 正式协作文档放在 `00-总览` 到 `07-安全`。
- 开发过程和学习复盘放在 `08-复盘`。
- 面试讲解、概念解释等辅助资料放在 `09-学习资料`。
- 被新路线图替代但仍有历史参考价值的旧计划放在 `99-归档`。
- 本次没有直接删除项目相关文档，避免误删仍有学习或追溯价值的内容。

### 19.3 本次移动的重点文档

- `docs/00-总览/项目上下文索引.md`
- `docs/01-产品/需求文档.md`
- `docs/01-产品/后续路线图.md`
- `docs/02-架构/架构说明.md`
- `docs/02-架构/技术决策记录.md`
- `docs/03-接口/接口说明.md`
- `docs/04-编辑器/事件动作规划.md`
- `docs/04-编辑器/事件绑定系统设计.md`
- `docs/05-开发/本地开发指南.md`
- `docs/06-测试与验收/功能演示流程.md`
- `docs/07-安全/安全规范.md`
- `docs/08-复盘/开发进度与学习总结.md`
- `docs/09-学习资料/后端与数据库说明.md`
- `docs/09-学习资料/面试讲解稿.md`
- `docs/99-归档/历史实施计划.md`

### 19.4 同步更新内容

- 更新 `README.md` 的文档入口。
- 更新 `AGENTS.md` 和 `CLAUDE.md` 的上下文入口。
- 更新 `.claude/context/FILE_MAP.md` 的关键文档路径。
- 更新 `.agents/skills/context-index/SKILL.md` 和 `.claude/skills/context-index/SKILL.md`，避免以后刷新上下文索引时写回旧路径。
- 清理 `CLAUDE.md` / `AGENTS.md` 中已经过时的事件动作名称和性能面板说明。

### 19.5 已验证内容

已运行：

```bash
npm.cmd run build
npm.cmd run build --prefix server
```

结果：

- 前端生产构建通过。
- 后端构建通过。
- 已检查旧文档路径引用，未发现旧路径残留。


## 20. 2026-05-07 企业级工程规范化计划

本次新增的是后续代码结构治理计划，先写规划，不直接移动前后端代码。

### 20.1 新增文档

```text
docs/05-开发/企业级工程规范化计划.md
```

### 20.2 规划重点

- 先在现有结构内建立边界，再考虑长期迁移到 `apps/` + `packages/` 的轻量 monorepo。
- 前端目标分层：`app`、`features`、`editor`、`shared`。
- 后端目标分层：`common`、`config`、`prisma`、`modules/<domain>`。
- 编辑器内部后续拆分：`events`、`schema`、`runtime`、`registry`、`setting`。
- 历史 typo API 采用兼容迁移，不一次性删除。
- 测试、CI、脚本、Docker/Nginx 等工程化能力分阶段补齐。

### 20.3 执行原则

这份计划的核心不是“为了企业级而大搬家”，而是先建立清晰边界和迁移节奏。后续每阶段都必须保证前端构建、后端构建和核心编辑器链路不回归。


## 21. 2026-05-07 工程规范化 P0/P1 起步

本次开始按 `docs/05-开发/企业级工程规范化计划.md` 执行第一批低风险整理，不改变编辑器业务行为。

### 21.1 应用入口分层

- 新增 `src/app/App.tsx` 作为应用级视图分发主入口。
- 新增 `src/app/providers/AppProviders.tsx`，集中应用 Provider，目前包含 React DnD Provider。
- `src/App.tsx` 改为兼容转发入口。
- 新增 `src/features/publish/PublishedPageView.tsx`，承接公开发布页运行态渲染。

### 21.2 历史拼写兼容层

- `src/editor/components/Preview/index.tsx` 成为预览渲染主实现。
- `src/editor/components/Preivew/index.tsx` 保留为兼容导出。
- `src/editor/hooks/useMaterialDrop.ts` 成为拖拽 hook 主实现。
- `src/editor/hooks/useMaterailDrop.ts` 保留为兼容导出。
- `src/editor/components/Sourse/index.tsx` 保留为 `Source` 的兼容导出。
- 新增代码优先使用 `Preview`、`Source`、`useMaterialDrop` 正确命名入口。

### 21.3 工程检查

- 根目录新增 `npm run check`，统一执行 lint、前端构建和后端构建。
- 更新 `docs/05-开发/编码规范.md`、`docs/00-总览/项目上下文索引.md`、`.claude/context/FILE_MAP.md`、`AGENTS.md`、`CLAUDE.md`、`docs/02-架构/架构说明.md`。


## 22. 2026-05-07 前端应用层 P1 拆分

本次继续降低 `src/app/App.tsx` 复杂度，把应用入口从“集中处理所有状态和副作用”拆成可维护的小模块。

### 22.1 新增模块

- `src/app/hooks/useAuthSession.ts`：负责登录态初始化、读取当前用户、token 失效后的登出处理。
- `src/app/hooks/useEditorPageLoader.ts`：负责打开页面、读取 page schema、写入 `useComponetsStore`。
- `src/app/components/AppViewOutlet.tsx`：根据 `AppView` 渲染登录页、项目面板或编辑器。
- `src/app/components/AppLoading.tsx`：统一应用级加载态。
- `src/app/routes/initialView.ts`：集中初始视图判断。

### 22.2 保持不变的行为

- `/publish/:publicId` 仍然绕过登录初始化。
- 有 token 时仍会调用 `getCurrentUser()` 校验会话。
- 打开页面仍是 `GET /api/pages/:id` 后写入编辑器组件树，再进入编辑器。
- 退出登录仍清理 token 并回到登录页。

### 22.3 已验证内容

已先运行：

```bash
npm.cmd run lint
```

结果：前端 lint 通过。


## 23. 2026-05-07 前端 API 层 P1 整理

本次完成 P1 后半段，把前端 API 主实现下沉到 `shared` 层。

### 23.1 调整内容

- 新增 `src/shared/api/http.ts`、`auth.ts`、`projects.ts`、`pages.ts`、`types.ts`、`index.ts`。
- `src/api/*` 改为兼容导出，避免历史引用立刻失效。
- `src/app`、`src/features`、`src/editor/components/Header` 的 API 引用迁移到 `src/shared/api/*`。

### 23.2 目的

- 让 `app`、`features`、`editor` 都依赖稳定的公共 API 层。
- 为后续继续拆 `features` 和抽共享类型做准备。
- 避免根级 `src/api` 继续成为业务代码随手依赖的旧入口。

### 23.3 已验证内容

已先运行：

```bash
npm.cmd run lint
```

结果：前端 lint 通过。


## 24. 2026-05-07 编辑器事件模块 P2 拆分

本次进入 P2 第一块，把原来的 `src/editor/events.ts` 拆成更清晰的事件领域目录，运行逻辑保持不变。

### 24.1 新目录

```text
src/editor/events/types.ts
src/editor/events/eventNames.ts
src/editor/events/normalize.ts
src/editor/events/createEventData.ts
src/editor/events/runtime.ts
src/editor/events/index.ts
```

`src/editor/events.ts` 保留为兼容导出，避免旧引用立刻失效。

### 24.2 拆分边界

- `types.ts`：事件动作类型和运行上下文。
- `eventNames.ts`：低代码事件名与 React 事件 prop 转换。
- `normalize.ts`：旧事件 schema 兼容、动作 normalize、URL normalize。
- `createEventData.ts`：按事件类型生成 `eventData`。
- `runtime.ts`：动作执行器，包括 toast、url、confirm、condition、http、componentAction、custom。

### 24.3 已验证内容

已先运行：

```bash
npm.cmd run lint
```

结果：前端 lint 通过。


## 25. 2026-05-07 编辑器 runtime 与 registry P2 整理

本次继续完成 P2 的目录边界整理，把运行态预览和物料注册表从旧位置迁到更准确的领域目录。

### 25.1 runtime

- `src/editor/runtime/Preview/index.tsx` 成为预览/公开运行态渲染主实现。
- `src/editor/components/Preview/index.tsx` 保留为兼容导出。
- `src/editor/components/Preivew/index.tsx` 继续保留历史拼写兼容导出。

### 25.2 registry

- `src/editor/registry/component-config.tsx` 成为物料注册表主实现。
- `src/editor/stores/component-config.tsx` 保留为兼容导出。
- 编辑器内部引用已迁移到 `registry/component-config`。

### 25.3 已验证内容

已先运行：

```bash
npm.cmd run lint
```

结果：前端 lint 通过。


## 26. 2026-05-07 后端模块增强 P3

本次进入 P3，重点增强后端工程边界，不改变现有 API 路由。

### 26.1 统一错误码

- 新增 `server/src/common/errors/error-codes.ts`。
- 新增 `server/src/common/errors/business.exception.ts`。
- `HttpExceptionFilter` 响应中补充 `code`、`path`、`method`。
- Auth、Projects、Pages 的关键业务错误改为带业务码的异常。

### 26.2 环境变量校验

- 新增 `server/src/config/env.validation.ts`。
- 启动时校验 `DATABASE_URL`、`JWT_SECRET` 必填。
- 校验 `PORT` 必须是合法端口。
- 校验 `FRONTEND_ORIGIN` 必须是合法 URL。

### 26.3 Pages 模块拆分

- `page-schema.service.ts`：默认 schema 和 normalize。
- `page-versions.service.ts`：版本创建、列表、回滚、删除。
- `page-publish.service.ts`：发布、取消发布、公开读取发布快照。
- `pages.service.ts`：保留页面 CRUD、owner 校验和领域编排。

### 26.4 已验证内容

已先运行：

```bash
npm.cmd run build --prefix server
```

结果：后端构建通过。


## 27. 2026-05-07 P3/P4 共享 Schema 与服务端校验

本次继续完成 P3 剩余项，并进入 P4，目标是减少前后端 schema 校验漂移。

### 27.1 共享包

新增：

```text
packages/lowcode-schema/src/types.ts
packages/lowcode-schema/src/registry.ts
packages/lowcode-schema/src/validate.ts
packages/lowcode-schema/src/index.ts
```

共享包提供：

- `LowcodeComponentSchema`
- `LowcodePageSchema`
- `builtinComponentSchemaRegistry`
- `validateComponentTree`
- `assertValidComponentTree`

### 27.2 前端接入

- `src/editor/schema/validateComponents.ts` 改为共享校验器的兼容出口。
- 源码应用、保存、发布前仍沿用原入口，但实际校验逻辑来自 `packages/lowcode-schema`。

### 27.3 后端接入

- `server/src/modules/pages/page-schema.service.ts` 在 normalize 前校验 `schema.components`。
- 创建、保存、发布、回滚都会经过 `PageSchemaService`，因此非法 schema 会在后端被阻断。
- 新增错误码：`PAGE_SCHEMA_INVALID`。

### 27.4 已验证内容

已先运行：

```bash
npm.cmd run lint
npm.cmd run build
npm.cmd run build --prefix server
```

结果：前端 lint、前端构建、后端构建均通过。


## 28. 2026-05-07 P4 Schema Migration 完成

本次继续完成 P4，把共享 schema 包从“类型和校验”推进到“默认值、版本号、迁移、校验”完整闭环。

### 28.1 新增能力

```text
旧页面 schema / 缺省 schema / 旧事件字段
  ↓
packages/lowcode-schema/src/migrate.ts
  ↓
schemaVersion = CURRENT_SCHEMA_VERSION
  ↓
补齐默认 Page、props、desc、metadata
  ↓
旧事件 props.onClick.actions → props.onEvent.click.actions
  ↓
再进入前端运行态或后端入库校验
```

### 28.2 本次修改文件

共享 schema：

```text
packages/lowcode-schema/src/defaults.ts
packages/lowcode-schema/src/migrate.ts
packages/lowcode-schema/src/index.ts
```

作用：

- 定义 `CURRENT_SCHEMA_VERSION` 和默认 Page schema。
- 下沉事件动作类型，让 `LowcodeAction`、`ActionType`、`EventCategory` 等结构也来自共享包。
- 新增 `migratePageSchema`，集中处理缺省 schema、旧组件字段和旧事件动作结构。
- 异常组件 id 使用稳定递增分配，避免 `Date.now()` 带来的重复和不确定性。

前端接入：

```text
src/app/hooks/useEditorPageLoader.ts
src/features/publish/PublishedPageView.tsx
src/editor/components/Header/index.tsx
src/editor/stores/components.tsx
src/shared/api/types.ts
```

作用：

- 打开页面、回滚版本、公开发布页渲染前统一执行 schema migration。
- Zustand 本地缓存恢复和 `setComponents` 入口也执行 migration，并统一回到编辑模式。
- 保存页面前把编辑器内部 `Component` 序列化为共享 `LowcodePageSchema`，避免 React `CSSProperties` 泄漏到 API schema 类型里。
- `PageSchema` 类型复用共享包，减少前后端类型漂移。

后端接入：

```text
server/src/modules/pages/page-schema.service.ts
```

作用：

- 创建、保存、发布、回滚都统一先调用 `migratePageSchema`，再调用共享校验器。
- 后端不再维护本地重复默认 schema。

文档：

```text
docs/00-总览/项目上下文索引.md
docs/02-架构/架构说明.md
docs/03-接口/接口说明.md
docs/05-开发/企业级工程规范化计划.md
.claude/context/FILE_MAP.md
docs/08-复盘/开发进度与学习总结.md
```

作用：同步 P4 当前状态、schema migration 接入路径和关键文件风险。

### 28.3 已验证内容

已运行：

```bash
npm.cmd run check
```

结果：

- 前端 lint 通过。
- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- Vite 仍有 chunk size warning，不影响构建结果。

### 28.4 你应该学习的点

#### 迁移和校验要分工

迁移负责把旧数据变成当前系统能理解的形状，校验负责判断迁移后的数据是否满足规则。两者不要混在组件运行态里，否则每个入口都会有一套隐形兼容逻辑。

#### 前后端共享 schema 能减少漂移

当前 `PageSchema`、默认 schema、物料父子规则和校验器都在 `packages/lowcode-schema`，前端和后端引用同一份规则。以后新增物料或改 schema 结构时，优先改共享包，再同步编辑器 registry 和文档。

#### 本地缓存也是旧数据来源

除了数据库里的旧页面，浏览器 localStorage 里的 Zustand persist 也可能保存旧结构。把 persist 恢复接入 migration，可以减少“换了 schema 后本地一打开就异常”的问题。


## 29. 2026-05-07 P5 工程化基础

本次完成 P5 的第一版工程化闭环：测试、CI、API smoke、Docker/Nginx 模板和部署基础文档。

### 29.1 新增能力

```text
本地开发
  ↓
npm.cmd run check
  ↓
lint + 前端构建 + 后端构建 + schema 单测
  ↓
GitHub Actions 自动执行
```

```text
本地后端和数据库已启动
  ↓
npm.cmd run smoke:api
  ↓
注册 / 登录 / 创建项目 / 创建页面 / 保存 schema / 发布 / 公开读取
```

### 29.2 本次修改文件

测试和脚本：

```text
scripts/test/schema.test.mjs
scripts/smoke/api-smoke.mjs
package.json
eslint.config.js
```

作用：

- 使用 Node 内置 test runner，避免新增外部测试依赖。
- 单测覆盖 schema migration、旧事件字段迁移、稳定 id 分配、schema 校验和 URL normalize。
- API smoke 覆盖后端核心闭环。
- `npm.cmd run check` 现在会把单测纳入质量门禁。

CI：

```text
.github/workflows/ci.yml
```

作用：在 push 和 pull request 时执行依赖安装与 `npm run check`。

部署基础：

```text
.dockerignore
infra/docker/Dockerfile.web
infra/docker/Dockerfile.server
infra/nginx/web.conf
infra/docker/docker-compose.prod.example.yml
docs/05-开发/CI与部署基础.md
```

作用：

- 增加前端 Nginx 静态服务镜像。
- 增加后端 NestJS 镜像。
- 增加 `/api` 反向代理和 SPA fallback。
- 增加测试环境 compose 示例。
- 记录本地测试、API smoke、CI 和 Docker 使用方式。

后端启动脚本：

```text
server/package.json
```

作用：修正生产启动入口为 `dist/server/src/main.js`，并增加 `prisma:deploy` 脚本。共享包进入构建后，后端产物路径不再是旧的 `dist/main.js`。

### 29.3 已验证内容

已运行：

```bash
npm.cmd run test
npm.cmd run check
npm.cmd run smoke:api
```

结果：

- 前端 lint 通过。
- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- Node test runner 共 7 个用例全部通过。
- API smoke 返回 `ok: true`，验证了注册、登录、创建项目、创建页面、保存 schema、版本生成、发布、公开读取和取消发布的核心闭环。

### 29.4 你应该学习的点

#### 工程化不是只加配置文件

CI、Docker、测试脚本必须和真实项目命令连起来。否则文件看起来很完整，但没有保护实际开发流程。

#### Smoke test 和单元测试分工不同

单元测试验证纯函数规则，例如 schema migration。Smoke test 验证系统闭环，例如注册、保存、发布和公开读取。它们解决的是不同风险。

#### Docker 模板还不是生产方案

当前 Docker/Nginx 是测试环境基础模板。真正生产还需要 HTTPS、日志、备份、镜像版本、密钥管理、迁移流程和回滚策略。

## 30. P2 编辑器体验收口

### 30.1 本次完成

- 画布增加桌面、平板、手机宽度切换。
- 选中浮层和右键菜单支持复制、删除、上移、下移、包裹容器。
- 组件 store 补齐重命名、复制、同级移动、按位置移动、包裹容器等结构操作。
- 右侧配置面板增加组件摘要、统一搜索、分组折叠和空状态。
- 源码区增加格式化、复制、重置、应用前变更摘要确认。
- 物料区增加收藏和常用模板。
- 大纲树增加定位、拖拽排序和重命名。

### 30.2 关联文档

```text
docs/01-产品/后续开发实施计划.md
docs/04-编辑器/编辑器体验优化说明.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
```

### 30.3 已验证内容

已运行：

```bash
npm.cmd run check
npm.cmd run test:e2e
```

结果：

- 前端 lint 通过。
- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- Node test runner 共 32 个用例全部通过。
- Playwright E2E 通过，覆盖 mock 的编辑、预览、保存、发布和公开页流程。
- Vite 仍提示大 chunk 警告，后续放入 P5 拆包优化。

## 31. P3 物料体系扩展收口

### 31.1 本次完成

- 新增基础物料：Link、Icon、Space、Flex、Grid、Tabs、Steps。
- 新增表单物料：Textarea、Radio、Checkbox、DatePicker、Upload、Rate。
- 新增数据物料：List、Descriptions、Statistic、Pagination、Chart。
- 新增反馈物料：Drawer、Tooltip、Popover、Notification、Result、Empty。
- 增强 Form：支持更多 FormItem 类型、默认值、校验、提交/重置、valuesChange 和组件方法。
- 增强 Table：支持静态/远程数据源、分页、空状态、日期列和操作列。
- 物料区模板补齐表单页、详情页、列表页、弹窗表单和仪表盘。
- 共享 schema registry 已同步 P3 物料父子关系。

### 31.2 关联文档

```text
docs/01-产品/后续开发实施计划.md
docs/04-编辑器/物料体系说明.md
docs/04-编辑器/事件能力矩阵.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
```

### 31.3 已验证内容

已运行：

```bash
npm.cmd run check
npm.cmd run test:e2e
```

结果：

- 前端 lint 通过。
- 前端 TypeScript 和 Vite 生产构建通过。
- 后端 NestJS 构建通过。
- Node test runner 共 34 个用例全部通过，新增 P3 schema、事件数据和预览渲染覆盖。
- Playwright E2E 通过，覆盖 mock 的编辑、预览、保存、发布和公开页流程。
- Vite 大 chunk 警告继续存在，并且 P3 引入更多 Ant Design 组件后包体增加，后续 P5 需要优先拆包。

## 32. P4 后端协作权限与审计第一轮

### 32.1 本次完成

- 后端从单 owner 判断升级为 `ProjectMember` 成员模型，支持 `owner/editor/viewer`。
- 新增 `ProjectAccessService`，统一项目和页面接口的角色解析，避免权限判断散落在多个 service。
- 新增项目成员 API：成员列表、添加成员、变更角色、移除成员。
- 新增 `AuditLog` 审计模型和 `AuditLogsService`，记录项目、成员、页面保存、发布、取消发布、回滚、删除等关键操作。
- 页面接口按角色重新约束：viewer 只读，editor/owner 可保存、发布、回滚和删除版本。
- API smoke 扩展为三用户协作验证：owner 邀请 editor/viewer、viewer 写入被拒、editor 可编辑、降级后失去写权限、owner 可查看审计。

### 32.2 关联文档

```text
docs/02-架构/架构说明.md
docs/02-架构/技术决策记录.md
docs/03-接口/接口说明.md
docs/00-总览/项目上下文索引.md
.claude/context/FILE_MAP.md
```

### 32.3 待继续

- 成员管理前端 UI。
- owner 转移和邀请确认流程。
- 页面版本差异对比。
- 页面模板、区块模板和素材管理后端存储。

### 32.4 已验证内容

已运行：

```bash
npm.cmd --prefix server run prisma:generate
npm.cmd --prefix server run prisma:deploy
npm.cmd run smoke:api
npm.cmd run check
```

结果：

- Prisma Client 生成成功。
- 数据库 migration `20260508090000_add_project_members_and_audit_logs` 已应用到本地 PostgreSQL。
- API smoke 通过，覆盖三用户成员权限、页面保存发布和审计日志。
- 前端 lint、前端生产构建、后端 NestJS 构建和 34 个 Node 测试全部通过。
- Vite 大 chunk 警告仍存在，继续留给 P5 拆包优化。

